import 'package:flutter/material.dart';
class AddJobOfferPage extends StatelessWidget {
  const AddJobOfferPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('AddJobOfferPage')), body: const Center(child: Text('En construction...')));
  }
}
