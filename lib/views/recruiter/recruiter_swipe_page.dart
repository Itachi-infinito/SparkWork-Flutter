import 'package:flutter/material.dart';
class RecruiterSwipePage extends StatelessWidget {
  const RecruiterSwipePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('RecruiterSwipePage')), body: const Center(child: Text('En construction...')));
  }
}
