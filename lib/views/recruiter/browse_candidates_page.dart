import 'package:flutter/material.dart';
class BrowseCandidatesPage extends StatelessWidget {
  const BrowseCandidatesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('BrowseCandidatesPage')), body: const Center(child: Text('En construction...')));
  }
}
