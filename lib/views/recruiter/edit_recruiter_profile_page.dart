import 'package:flutter/material.dart';
class EditRecruiterProfilePage extends StatelessWidget {
  const EditRecruiterProfilePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('EditRecruiterProfilePage')), body: const Center(child: Text('En construction...')));
  }
}
