import 'package:flutter/material.dart';
class RecruiterMatchesPage extends StatelessWidget {
  const RecruiterMatchesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('RecruiterMatchesPage')), body: const Center(child: Text('En construction...')));
  }
}
