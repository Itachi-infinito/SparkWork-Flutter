import 'package:flutter/material.dart';
class RecruiterJobOffersPage extends StatelessWidget {
  const RecruiterJobOffersPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('RecruiterJobOffersPage')), body: const Center(child: Text('En construction...')));
  }
}
