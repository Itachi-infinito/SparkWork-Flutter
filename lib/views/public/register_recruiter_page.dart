import 'package:flutter/material.dart';
class RegisterRecruiterPage extends StatelessWidget {
  const RegisterRecruiterPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('RegisterRecruiterPage')), body: const Center(child: Text('En construction...')));
  }
}
