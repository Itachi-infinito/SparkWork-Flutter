import 'package:flutter/material.dart';
class RegisterCandidatePage extends StatelessWidget {
  const RegisterCandidatePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('RegisterCandidatePage')), body: const Center(child: Text('En construction...')));
  }
}
