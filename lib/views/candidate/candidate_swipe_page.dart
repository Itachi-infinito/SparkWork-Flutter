import 'package:flutter/material.dart';
class CandidateSwipePage extends StatelessWidget {
  const CandidateSwipePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('CandidateSwipePage')), body: const Center(child: Text('En construction...')));
  }
}
